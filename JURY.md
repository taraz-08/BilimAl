# ЖЮРИ БАҒАЛАУ ПАРАҒЫ
## Аймақтық AI-хакатон «AI for Kazakhstan: Industry 5.0 Solutions»

**Команда:** BilimAI
**Трек:** Білім беру

---

## ⚙️ I. ТЕХНИКАЛЫҚ БЛОК
**Салмақ: 40% | Максимум: 40 балл**

| Критерий | Баға (1–10) | Жоба бойынша негіздеме |
|---|---|---|
| **MVP жұмысқа қабілеттілігі** | — | Толық жұмыс жасайтын өнім: авторизация, тест генерациясы, прокторинг, OCR, ұсыныстар — «заглушка» жоқ |
| **Күрделілік және логика** | — | 5 AI-модуль: Gemini (генерация + тексеру + ұсыныстар), Vision API (OCR), face-api.js (бет анықтау), COCO-SSD (телефон анықтау), ML-тәуекел болжамы |
| **Код сапасы және Git** | — | Таза модульдік архитектура: routers / services / models. Pydantic схемалары, SQLAlchemy ORM, JWT-аутентификация, қате өңдеу |
| **Шешім архитектурасы** | — | FastAPI + React + SQLite (→ PostgreSQL). Клиенттік AI-модельдер — студент жеке деректері серверге жіберілмейді. OpenAPI документация |

**I блок жиыны:** _____ / 40

---

## 💼 II. БИЗНЕС-ҚҰНДЫЛЫҚ ЖӘНЕ ҚОЛДАНЫЛУЫ
**Салмақ: 20% | Максимум: 20 балл**

| Критерий | Баға (1–10) | Жоба бойынша негіздеме |
|---|---|---|
| **Кейсті шешу** | — | Оқытушының күнделікті жұмысын автоматтандырады (тесттер, тексеру, журнал), емтиханда адалдықты арттырады, студентке қазақ тілінде жеке AI-талдау береді |
| **Енгізудің нақтылығы** | — | Браузер арқылы жұмыс жасайды, орнату қажет емес. 1514-15 ИП тобының нақты деректерінде тексерілген (17 студент, 7 сабақ). SaaS-модель дайын |

**II блок жиыны:** _____ / 20

---

## 💡 III. ИННОВАЦИЯ ЖӘНЕ ШЫҒАРМАШЫЛЫҚ
**Салмақ: 15% | Максимум: 15 балл**

| Критерий | Баға (1–10) | Жоба бойынша негіздеме |
|---|---|---|
| **Стектің тиімділігі** | — | Бірегей үйлесім: клиенттік прокторинг (видео жазылмайды, жеке өмір қорғалады), Vision API арқылы қолжазбаның OCR, толық қазақ тілді AI (Gemini prompt engineering). Қазақстандық EdTech-те аналогы жоқ |

**III блок жиыны:** _____ / 15

---

## 🎤 IV. ПИТЧ ЖӘНЕ ДИЗАЙН
**Салмақ: 25% | Максимум: 25 балл**

| Критерий | Салмақ | Баға (1–10) | Негіздеме |
|---|---|---|---|
| **Питч сапасы** | 15% | — | Оқытушы мен студент үшін құндылық нақты бөлінген. Live demo: тест генерациясы → тапсыру → нәтиже → қолжазба OCR |
| **UX/UI Дизайн** | 10% | — | Material Design 3, адаптивтілік, Framer Motion анимациялары, үш тіл (ҚАЗ/РУС/ЕНГ), қараңғы тема |

**IV блок жиыны:** _____ / 25

---

## 🏅 V. ТЕХНОЛОГИЯЛЫҚ БОНУСТАР
**Жалпы балға +5% дейін**

| | Критерий | BilimAI жағдайы |
|---|---|---|
| ☐ | Жергілікті LLM интеграциясы (тек API шақыру емес) | Қазақ тілінде кастомды prompt engineering арқылы Gemini API қолданылады |
| ☐ | Hardware / Робототехника | — |
| ☑ | **Қазақстандық деректер жинағы** | СКПУ студенттерінің нақты деректері: 1514-15 ИП тобы, қазақстандық транскрипт форматын парсинг жасау |

---

## ЖАЛПЫ БАЛЛ: ________ / 105

---

## 🔍 СУДЬЯЛАРДЫҢ МІНДЕТТІ СҰРАҚТАРЫНА ЖАУАПТАР

### ▸ «Репозиторийді ашыңыз. AI логикасына жауапты кодты көрсетіңіз. Неліктен дәл осы әдісті таңдадыңыз?»

**Файл:** `backend/services/ai_grading.py`

```python
# Промпт толностью на казахском — Gemini-ді қазақша жауап беруге мәжбүрлейді
prompt = f"""Сіз академиялық бағалаушысыз. Студенттің жазба жауабын бағалаңыз.
Сұрақ: {question}
Студент жауабы: {answer}
МАҢЫЗДЫ: feedback өрісін міндетті түрде ҚАЗАҚ тілінде жазыңыз.
JSON форматында жауап беріңіз: {{"score": <сан>, "feedback": "<мәтін>"}}"""
```

**Неліктен Gemini 2.5 Flash:** GPT-4-тен 4 есе арзан, жылдамырақ, дұрыс prompt engineering арқылы қазақ тілімен нативті жұмыс жасайды. GPT-4 қазақша жауап беру нұсқаулығын орындамады — Gemini дұрыс орындады.

**Файл:** `frontend/src/hooks/useProctor.js`

```javascript
// Сервер шығынсыз клиенттік телефон анықтау
const predictions = await phoneModelRef.current.detect(videoRef.current)
const phone = predictions.find(p => p.class === 'cell phone' && p.score > 0.5)
if (phone) {
  const screenshot = captureScreenshot() // canvas → base64
  addViolation('phone_detected', `Телефон анықталды`, screenshot)
}
```

**Неліктен клиенттік модель:** Видеоағын студент құрылғысынан шықпайды → жеке өмір қорғалады, видео өңдеуге серверлік шығын жоқ.

---

### ▸ «Қандай кітапханалар қолдандыңыз және үйлесімділік мәселесін қалай шештіңіз?»

**1-мәселе — google.generativeai ескірді:**
Бастапқыда `google.generativeai` + `gemini-1.5-flash` → 404 қате алдық. Жаңа `google.genai` SDK + `gemini-2.5-flash` нұсқасына көшуге тура келді. Үш AI-сервисті толығымен қайта жаздық.

**2-мәселе — CORS + JWT:**
FastAPI-де `allow_origins=["*"]` мен `allow_credentials=True` үйлеспейді — барлық POST сұраулары CORS-пен бұғатталды. Шешім: `allow_credentials=False`, JWT тек `Authorization: Bearer` тақырыбымен жіберіледі, cookie емес.

**3-мәселе — face-api.js модельдері:**
jsDelivr CDN tinyFaceDetector модельдері үшін 404 қайтарды. GitHub raw-тен модельдерді қолмен жүктеп `public/models/` ішіне орналастырдық.

**4-мәселе — COCO-SSD + TensorFlow.js:**
`@tensorflow-models/coco-ssd` peer dependency ретінде `@tensorflow/tfjs` талап етеді. Бет жүктелуін бұғатламау үшін useEffect ішінде динамикалық `import()` арқылы жүктедік.

---

### ▸ «24 сағат ішінде AI интеграциясында қандай негізгі техникалық қиындықтарға тап болдыңыз?»

1. **Gemini JSON-ды \`\`\`json...\`\`\` ішіне орап қайтарды** — `json.loads()` алдында markdown-орамасын алып тастайтын парсер жаздық

2. **Gemini тегін тирде тәулігіне 20 сұрау квотасы** — graceful degradation қостық: `try/except` `429 RESOURCE_EXHAUSTED` ұстайды, роутер crash орнына қазақша хабармен HTTP 503 қайтарады

3. **AI жауаптарындағы қазақ тілі** — Gemini қазақша сұраққа да ағылшынша жауап берді. Шешім: промпт толностью қазақ тілінде жазылды + `МАҢЫЗДЫ: feedback өрісін міндетті түрде ҚАЗАҚ тілінде жазыңыз` нұсқаулығы

4. **SQLAlchemy enum 'graded' жоқ болды** — деректер базасын себіп жатқанда `IntegrityError` шықты. ORM-ді айналып өтіп `UPDATE grades SET status='approved'` raw SQL қолдануға тура келді

5. **Браузердің Fullscreen API** — Safari-де `requestFullscreen()` тікелей user gesture талап етеді, `Promise.finally` ішінен шақыруға болмайды. Бөлек клик өңдеушіге орауға тура келді

---

### ▸ «Өзгерістер тарихын көрсетіңіз. Негізгі жұмыс қай уақытта жасалды?»

Жоба хакатон аясында әзірленді. Негізгі модульдер дәйекті түрде іске асырылды:

1. Базалық архитектура (FastAPI + React + Auth)
2. Тест жүйесі (CRUD + генерация)
3. AI интеграциялары (Gemini + Vision API)
4. Прокторинг (face-api.js + COCO-SSD)
5. Журнал + транскрипт (СКПУ PDF парсинг)
6. Қолжазба OCR (Vision API + Gemini бағалау)

---

### ▸ «Неліктен дәл осы стекті (FastAPI) таңдадыңыз, басқасын емес?»

| Нұсқа | Неліктен таңдамадық |
|---|---|
| Django | API-only сервис үшін артық, баяу, нативті async жоқ |
| Node.js / Express | AI интеграциялары үшін команда Python-да күштірек |
| Flask | Нативті валидация жоқ, автодокументация жоқ |
| **FastAPI** ✅ | Pydantic-валидация, OpenAPI docs автоматты, async қолдауы, AI-микросервистерге тамаша |

**React + Vite, Next.js емес:**
SSR қажет емес — JWT-авторизациялы SPA. Vite HMR < 300ms береді, бұл 24 сағаттық әзірлеу кезінде маңызды.

**SQLite, PostgreSQL емес:**
Хакатонда — жылдам орналастыру. Production-да — `config.py`-дағы бір жол код өзгертпестен PostgreSQL-ге ауыстырады (SQLAlchemy абстракция).

---

*Судья қолы: _________________________*
*Аты-жөні: _________________________*
